import 'package:flutter/material.dart';

import '../constants/colors.dart';

BoxDecoration kHederDecoration = BoxDecoration(
  gradient: LinearGradient(
    colors: [
      Colors.transparent,
      CustomeColor.bgLight1,
    ],
  ),
  borderRadius: BorderRadius.circular(100.0),
);
